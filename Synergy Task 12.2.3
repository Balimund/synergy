# coding=windows-1251
# Ввод сумм инвестиций от Майкла и Ивана
A = float(input("Сумма инвестиций Майкла (A): "))
B = float(input("Сумма инвестиций Ивана (B): "))
X = float(input("Минимальная сумма инвестиций (X): "))

# Проверка возможностей инвестиций и вывод результата
if A >= X and B >= X:
    print(2)  # Оба могут вложиться
elif A >= X:
    print("Mike")  # Только Майкл может вложиться
elif B >= X:
    print("Ivan")  # Только Иван может вложиться
elif A + B >= X:
    print(1)  # Вместе им хватает, но по отдельности нет
else:
    print(0)  # Никто не может вложиться
